services:
  neutarr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: neutarr
    ports:
      - "9705:9705"
    volumes:
      - ./config:/config
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
    restart: unless-stopped
